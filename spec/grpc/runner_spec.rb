require 'spec_helper'

describe GRPC::Runner do
  it 'has a version number' do
    expect(GRPC::Runner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
